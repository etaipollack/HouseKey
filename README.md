# HouseKey
Wi-Fi controlled DC motor using a Raspberry Pi Zero W and L298N motor driver. Exposes a Flask HTTP endpoint to receive JSON POST requests and move the motor a configurable amount. Includes step simulation, direction control, and speed control with PWM.
